apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My automation
on:
  push:
    branches:
      - "**"
jobs:
  build:
    steps:
      - uses: docker://rockymoyosu/pymod:latest
        name: Say hello
        kind: deploy
        shell: sh
        run: ./entrypoint
